lst = []

def init():

    return

def solve():
    init()

    n = int(input())
    n = abs(n)
    
    if(n != 0):
        for i in range(1, int(n**0.5) + 1, 1):
            if(n % i == 0):
                lst.append(i)
                if(i*i != n): lst.append(n//i)
        
        lst.sort(reverse = True)
        for elm in lst:
            print(elm, end= " ")
    else: print("INF")

    return

solve()
