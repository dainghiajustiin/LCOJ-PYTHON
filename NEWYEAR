def init():

    return

def solve():
    init()

    n = int(input())
    dct = {}

    for i in range(n):
        s = input()
        if(not s in dct):
            dct.update({s : True})

    print(len(dct))

    return

solve()
