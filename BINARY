def init():

    return

def solve():
    init()

    n = int(input())

    for mask in range(1 << n):
        for i in range(n-1, -1, -1):
            print((mask >> i) & 1, end="")
        print()

    return

solve()
