def init():

    return

def solve():
    init()

    n = int(input())
    lst = list(map(int, input().split()))

    lst.sort()
    ans = max(lst[0]*lst[1], lst[n-1]*lst[n-2])
    print(ans)

    return

solve()
