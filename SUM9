def calc(i, n):
    if(i == n): return 0.5
    return 1/(1 + calc(i + 1, n))

def init():

    return

def solve():
    init()

    T = int(input())
    for _ in range(T):
        n = int(input())
        ans = calc(1, n)
        print(f"{ans:.5f}")

    return

solve()
