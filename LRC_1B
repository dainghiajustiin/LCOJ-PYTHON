import sys
from array import array 

n = int(input())
lst = [int(x) for x in input().split()]
lst.sort()

ans = 0
L, R = 0, len(lst) - 1 

while(L < R):
    if(lst[L] + lst[R] >= 0):
        ans += 1
        R -= 1
    L += 1 

print(ans)
