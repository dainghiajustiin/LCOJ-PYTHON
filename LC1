from random import randint

def init():

    return

def solve():
    init()

    n = int(input())

    a = randint(1, 99)
    b = randint(a + 1, 111)
    c = n - a - b

    print(a, b, c)

    return

solve()
