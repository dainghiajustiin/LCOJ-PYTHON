def init():

    return

def solve():
    init()

    n = int(input())
    lst = list(map(int, input().split()))
    cnt = [0] * (10**4+5)

    for elm in lst:
        cnt[elm] += 1

    for i in range(10**4+1):
        if(cnt[i] > 0):
            print(i, cnt[i])
    return

solve()
